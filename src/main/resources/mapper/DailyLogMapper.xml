<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.example.weightmanagement.mapper.DailyLogMapper">

	<select id="findByPeriod"
		resultType="com.example.weightmanagement.entity.DailyLog">
		SELECT
		log_date AS logDate,
		weight_kg AS weightKg,
		sleep_hours AS sleepHours,
		snack_type AS snackType
		FROM
		daily_log
		WHERE
		log_date BETWEEN #{startDate} AND #{endDate}
		ORDER BY
		log_date DESC
	</select>

</mapper>